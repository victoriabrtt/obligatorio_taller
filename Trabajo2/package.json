{
  "name": "erc20-dapp",
  "version": "1.0.0",
  "description": "dApp para interactuar con un token ERC20",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "deploy-live": "hardhat run scripts/deploy.js --network sepolia",
    "serve": "npx serve ./web"
  },
  "keywords": [
    "ethereum",
    "web3",
    "dapp",
    "erc20"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.12.0",
    "serve": "^14.2.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.0"
  }
}
